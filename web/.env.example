# 前端环境变量示例。复制为 .env.local（开发，已被 gitignore）或 .env.production（构建）后按需修改。

# dev server 代理目标：/api 与 /public 转发到哪个后端。
# 默认 http://localhost:8082（Go 后端默认监听 8082）。
# 后端跑在局域网其他机器时覆盖，如 http://192.168.1.29:8082。
#VITE_API_PROXY_TARGET=http://localhost:8082

# API 统一前缀（生产部署在 nginx 后面分流用，如 /webreaper）；开发时留空走 Vite proxy。
#VITE_API_PREFIX=/webreaper
