# 后端问题反馈

> 前端开发过程中遇到的后端问题记录于此，等待后端处理。前端只做临时规避，不修改后端代码。

| 编号 | 日期 | 模块/接口 | 问题描述 | 前端临时规避 | 状态 |
|---|---|---|---|---|---|
| 1 | 2026-08-30 | 主体管理 GET /subjects?ownership=system | 23 号计划阶段 0 要求「官方主体区」（服务端缓存代理全量官方主体），该端点尚未提供；前端分身库目前以静态样例卡代替，无法展示真实官方主体 | 分身库用 CDN 样例卡占位；等端点就绪后接入双源（官方+个人）分身选择器 | 待后端确认 |
| 2 | 2026-08-30 | 统一提交 type=subject / timeline / compose | ① 分身注册缺可选「场景图 + 场景描述」字段（23 号计划 §2.1：链式生成 10s 形象视频的环境参考）。② 实测 192.168.1.29 后端：`timeline` 端点**已上线且联调通过**（真实任务返回逐句时间轴）；但 `type=compose` **未上线**——提交返回「不支持的生成类型: compose」，导致 B-Roll「合成成片」最后一步无法闭环。前端 BrollDrawer 已全部就绪，compose 部署后即可端到端联调 | 时间轴定位/挂片段已可用；合成按钮提交时会收到友好报错 | 待后端确认 |
| 3 | 2026-08-30 | 作品库 /merchant/works 标题编码 | 部分视频作品 title 存在**编码乱码**（形如 `♦ð▪ há♦...`，疑似写入时 UTF-8/GBK 混用），首页待处理作品与作品库直接显示乱码。前端已做兜底（剔除不可读字符后为空则显示「未命名作品」），但原始数据仍坏，影响检索与发布标题 | 前端 cleanWorkTitle 兜底显示占位标题；建议后端排查 works 写入链路的编码处理并修复存量数据 | 待后端确认 |
