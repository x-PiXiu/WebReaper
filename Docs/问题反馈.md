# 后端问题反馈

> 前端开发过程中遇到的后端问题记录于此，等待后端处理。前端只做临时规避，不修改后端代码。

| 编号 | 日期 | 模块/接口 | 问题描述 | 前端临时规避 | 状态 |
|---|---|---|---|---|---|
| 1 | 2026-08-30 | 主体管理 GET /subjects?ownership=system | 23 号计划阶段 0 要求「官方主体区」（服务端缓存代理全量官方主体），该端点尚未提供；前端分身库目前以静态样例卡代替，无法展示真实官方主体 | 分身库用 CDN 样例卡占位；等端点就绪后接入双源（官方+个人）分身选择器 | **端点已上线（08-31）**：`GET /api/v1/subjects` 缓存代理（5min TTL）实测通过；**实测发现 Vidu 官方库 183 个主体全部是特效/场景/道具素材主体，无口播数字人**——官方区产品形态待定案（25 号 §3.3 选项 A/B/C，建议 B：联动 B-Roll 素材生成） |
| 2 | 2026-08-30 | 统一提交 type=subject / timeline / compose | ① 分身注册缺可选「场景图 + 场景描述」字段（23 号计划 §2.1：链式生成 10s 形象视频的环境参考）。② 实测 192.168.1.29 后端：`timeline` 端点**已上线且联调通过**（真实任务返回逐句时间轴）；但 `type=compose` **未上线**——提交返回「不支持的生成类型: compose」，导致 B-Roll「合成成片」最后一步无法闭环。前端 BrollDrawer 已全部就绪，compose 部署后即可端到端联调 | 时间轴定位/挂片段已可用；合成按钮提交时会收到友好报错 | ① 前端已按 `params.scene_image/scene_description` 先行提交（08-31），服务端接住即生效（25 号阶段二）；② **已闭环（08-31 P0）**：真实根因是前端字段错误（发 `type`+顶层字段，服务端要 `sub_type`+`params`），已修复；后端已重新部署，路由/校验链实测通过，ffmpeg 合成真实验证 819ms |
| 3 | 2026-08-30 | 作品库 /merchant/works 标题编码 | 部分视频作品 title 存在**编码乱码**（形如 `♦ð▪ há♦...`，疑似写入时 UTF-8/GBK 混用），首页待处理作品与作品库直接显示乱码。前端已做兜底（剔除不可读字符后为空则显示「未命名作品」），但原始数据仍坏，影响检索与发布标题 | 前端 cleanWorkTitle 兜底显示占位标题；建议后端排查 works 写入链路的编码处理并修复存量数据 | 待后端确认 |
| 4 | 2026-08-31 | 作品库 /merchant/works | `ListWorks` 的 deliverableSubTypes **不含 compose**——B-Roll 合成的新成片不进服务端作品列表；且 WorkItem 无 parent_task_id 顶层字段（血缘只能从任务 params JSON 解析） | 前端已从生成任务（sub_type=compose）追加合成成片到作品列表，按 id 去重，服务端补上后自动融合；B-Roll 标记从任务血缘推导 | **已修复（08-31）**：deliverableSubTypes 加 compose + WorkItem.parent_task_id；前端 workaround 已删（详见 24 号 §5） |
| 5 | 2026-08-31 | 文案润色 POST /generation/transcript/rewrite | 只有 `raw_text + topic`，**无"润色需求"参数**——23 号计划 §3.1 要求用户可输入一句话需求（如"更口语化"）影响改写方向 | 前端暂把需求拼入 topic；建议加独立 requirement 字段 | **已修复（08-31）**：请求体加 requirement（只作用于 rewrite 版提示）；前端改传独立字段 |
| 6 | 2026-08-31 | 转写异步任务 GET /generation/transcript/extract/tasks/:id | 响应仅 `pending/done/error` 三态，**无阶段/进度字段**——前端只能按已耗时轮播模拟"解析→下载→识别"进度提示 | 保留耗时模拟（代码已注释标明）；任务返回 stage/progress 后替换为真实进度 | **暂缓**（需改异步任务存储，随 asset_url 异步化一批做） |
| 7 | 2026-08-31 | 音色列表 GET /generation/voices | 音色对象仅 voice_id/language/name/sample_url 四字段，**无推荐/精选标记**——前端"推荐"Tab 只能取官方列表前 12 条本地截断 | 保留 slice(0,12)；表加 recommend 字段后改用服务端标记 | **已修复（08-31）**：077 迁移+seed 打标 8 个口播精选；前端消费服务端标记（旧库兜底） |
| 8 | 2026-08-31 | 素材库配额 | `/billing/usage` 的 scene 集合（monitor/generation/...）**无素材维度**，素材库用量上限前端只能写死 200 展示「视频 x/200」 | 保留写死展示；建议加 media 素材场景或独立配额端点 | 待后端确认 |
| 9 | 2026-08-31 | 主体链式形象视频（23 号 §2.1②） | subject 是同步端点（注册即 success）：**无链式 10s 形象视频任务、无形象视频 URL 回写、无「形象视频生成中」中间态**——前端三态徽标与形象视频预览弹窗无数据支撑 | 徽标维持 创建中/失败/可用；预览暂缺 | **评审已落地（08-31，25 号阶段二）**：链式设计+三态推导+决策点 D1-D5 待评审后实施 |
